import React from "react";

// function name must be capital
// better to make filename as function name
function Basicfuncomp() {
    // return section is a view
    // whatever we write here is jsx (not html)
    return(
        <h1> This is written in jsx
        and this is basic function component</h1>
    )
}

// export this so as to import into App.js (it is kind of home page)
export default Basicfuncomp;

